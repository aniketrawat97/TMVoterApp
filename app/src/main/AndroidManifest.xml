<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aniket.tmvoter">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ThankyouActivity"></activity>
        <activity android:name=".VoteTableTopicSpeaker" />
        <activity android:name=".AddPreparedSpeaker" />
        <activity android:name=".AddTableTopicSpeakerActivity" />
        <activity android:name=".UseAsActivity" />
        <activity android:name=".AddAuxilaryRoleplayerActivity" />
        <activity android:name=".AddRoleDynamic" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".MeetingCodeActivity" />
        <activity android:name=".GuestActivity" />
        <activity android:name=".GeneratingCodeActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".VoteRoleplayerActivity" />
        <activity android:name=".VoteAuxilaryRoleplayerActivity" />
        <activity android:name=".VoteEvaluaterActivity" />
        <activity android:name=".VotePrepSpeakerActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>